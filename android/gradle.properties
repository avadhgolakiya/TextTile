org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableR8.fullMode=true
android.enableMultiDex=true
# Disable Kotlin incremental compilation — fixes cross-drive path bug
# (project on G: drive, Pub cache on C: drive causes Kotlin daemon to crash)
kotlin.incremental=false
# Redirect build output to C: drive so NDK tools can handle paths (avoids cross-drive path bug)
project.buildDir=C:/Users/gulab/flutter_builds/saarika
