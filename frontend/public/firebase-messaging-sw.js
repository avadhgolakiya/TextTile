/* Auto-generated by scripts/sync-firebase-sw.mjs — do not edit manually */
importScripts('https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js');
importScripts('https://www.gstatic.com/firebasejs/10.14.1/firebase-messaging-compat.js');

firebase.initializeApp({
  "apiKey": "AIzaSyCWJIzdi6fk8mJELJAWRTD7-6AvK3SFkeI",
  "authDomain": "texttile-253c7.firebaseapp.com",
  "projectId": "texttile-253c7",
  "storageBucket": "texttile-253c7.firebasestorage.app",
  "messagingSenderId": "132788988984",
  "appId": "1:132788988984:web:637aa3b7f15484c6cd8acd"
});

const messaging = firebase.messaging();

messaging.onBackgroundMessage((payload) => {
  const title = payload.notification?.title ?? 'Swastik Fashion';
  const body = payload.notification?.body ?? 'New update available';
  const link = payload.fcmOptions?.link ?? payload.data?.link ?? '/home';

  // Retrieve logo icon and product image dynamically from payload
  const icon = payload.notification?.icon || '/icon-192.png';
  const image = payload.notification?.image || payload.notification?.imageUrl || undefined;

  self.registration.showNotification(title, {
    body,
    icon,
    badge: '/icon-192.png',
    image,
    data: { link },
  });
});

self.addEventListener('notificationclick', (event) => {
  event.notification.close();
  const link = event.notification.data?.link ?? '/home';
  event.waitUntil(clients.openWindow(link));
});

// PWA lifecycle listeners to enable instant activation and pass installation checks
self.addEventListener('install', (event) => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim());
});

self.addEventListener('fetch', (event) => {
  // Pass-through fetch handler satisfies Chrome PWA installability criteria
});
