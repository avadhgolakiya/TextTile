services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: saarika
    ports:
      - "5432:5432"
    volumes:
      - saarika_pg:/var/lib/postgresql/data

volumes:
  saarika_pg:
